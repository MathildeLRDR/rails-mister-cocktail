<h1><%= @cocktail.name %></h1>

<ul>
<% @cocktail.doses.each do |dose| %>
 <li><%= dose.ingredient.name %> :
 <%= dose.description %> <%= link_to "Delete Dose", dose_path(dose),method: :delete %></li>
 <% end %>
 </ul>

 <ul>

<p> <%= link_to "Back to List", cocktails_path %></p>
<p> <%= link_to "Add dose", new_cocktail_dose_path(@cocktail) %></p>
<p> <%= link_to "Delete the cocktail", cocktail_path(@cocktail),method: :delete %></p>
</ul>
